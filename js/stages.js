// Parallax stage definitions
// The further the layer, the closer it is to the players

import cafe from './stages/cafe.js';
import park from './stages/park.js';
import campus from './stages/campus.js';

const stages = {
    cafe,
    park,
    campus
};

export default stages;